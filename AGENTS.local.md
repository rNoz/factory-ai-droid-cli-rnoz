# Project rules

- Work directly on `main`; do not leave feature branches as the final state.
- Prefer merging and pushing completed work to `origin/main` as soon as it is ready.
- After a branch is merged, delete it both locally and from `origin`.
